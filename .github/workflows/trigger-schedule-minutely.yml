name: Trigger - schedule minutely

on:
  push:
    branches:
      - main
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:


jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello from scheduled workflow"